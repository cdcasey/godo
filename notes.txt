  Build one complete feature at a time:
  1. DB + migrations
  2. User model + User repo (auth's dependency)
  3. Auth/JWT + Auth middleware
  4. Auth handlers (register/login)
  5. Main.go with just auth routes ← Working auth API
  6. Todo model + Todo repo
  7. Todo handlers + wire into main
  8. Polish (middleware, Docker, Makefile)

  Revised order:

  1. DB connection + migration runner
  2. Makefile ← Move here (test, migrate, build, run commands)
  3. User model + User repo
  4. Auth/JWT + Auth middleware
  5. Auth handlers (register/login)
  6. Main.go with auth routes
  7. Todo model + Todo repo
  8. Todo handlers
  9. Docker + docker-compose
  10. .env.example + docs
